{
  "name": "fixdescriptorkit-evm",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npm -ws run build",
    "test": "npm -ws run test",
    "dev": "npm --workspace apps/web run dev",
    "lint": "eslint .",
    "vercel-build": "npm run generate-abis && npm -w fixdescriptorkit-typescript run build && npm -w apps/web run build",
    "generate-abis": "cd contracts && forge build && cd .. && node scripts/generate-abis.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*",
    "contracts"
  ]
}
